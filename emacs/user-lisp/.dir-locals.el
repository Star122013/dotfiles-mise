;;; Directory Local Variables
;;; For all Emacs Lisp files in this directory:
;;; - Initialize packages (package-initialize) so Flycheck can resolve
;;;   variables from installed packages like Eglot.

((emacs-lisp-mode . ((flycheck-emacs-lisp-package-initialize . t))))
